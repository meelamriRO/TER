#include <iostream>
#include <vector>
#include <list>
#include <new>
#include <string>
#include <fstream>
#include "dirent.h" 



using namespace std; 



struct nood {
  int n;
  int c; 
};

struct graph{
  vector <list <nood> > neighbours;
  int N;
};

struct graph * GraphInit(struct graph *g, int NbNood); 
